<div class="row">
  <div class="column">
    <!-- Main information, edit it on theme/_config.yml -->
    <p class="owner"><%= theme.owner %></p>
    <p><%= theme.info %></p>
    <p><%= theme.desc %></p>
  </div>
</div>

<h3 class="mt-1">Recent Posts</h3>

<% site.posts.sort('date', 'desc').limit(theme.max_post).forEach(function(post){ %>
<div class="row">
  <div class="twelve columns left mb-3">
    <a class="post-title" href="<%- url_for(post.path) %>">
      <% if (post.title == "") { %>
        no title
      <% } else { %>
        <%- post.title %>
      <% } %>
    </a>
    <p class="bottom"><small><%- full_date(post.date, 'll') %></small></p>
  </div>
</div>
<% }) %>

<h4 class="mb-2 mt-1"> <a href="<%= config.root %>archives">All Posts</a></h4>
<!--
<h3 class="mt-1">Tags</h3>
  <% var tags = site.tags.toArray().sort(function(a, b){ return b.length - a.length || a.name.localeCompare(b.name); }); %>
  <p class="post-meta">
  <% tags.forEach(function(tag, i){ %><a href="<%- url_for(tag.path) %>"><%- tag.name %></a>&nbsp;<span style="font-weight: 600; font-size: 0.87em; color: color-mix(in srgb, currentColor 72%, transparent)">(<%= tag.length %>)</span><% if (i < tags.length - 1) { %>, <% } %><% }) %>
  </p> -->
<hr>